"""
🎯 КРАТКАЯ ПАМЯТКА: REST АРБИТРАЖ

✅ ЧТО ИЗМЕНИЛОСЬ:

1. OpportunityAnalyzer (opportunity_analyzer.py)
   - MIN_GROSS_SPREAD: 0.35% → 1.0%
   - MIN_NET_EDGE: 0.15% → 0.3%
   - MAX_DATA_AGE: 1000ms → 500ms
   - SLIPPAGE: 0.02% → 0.05%
   - Добавлен REST_EXECUTION_BUFFER: 0.1%

2. RestOptimizedStrategy (strategies/rest_optimized_strategy.py) — НОВАЯ
   - Фиксированный TP: 0.4%
   - Фиксированный SL: 0.3%
   - Trailing Stop: после 0.5% прибыли
   - Max Hold Time: 180 секунд
   - Простая логика без зависимости от истории

3. Config (config.py)
   - OPEN_THRESHOLD: 0.7% → 1.0%
   - MAX_SPREAD_OPEN: 15.0% → 8.0%
   - Добавлен STRATEGY_TYPE = 'rest_optimized'
   - Новые параметры: REST_TAKE_PROFIT_PCT, REST_STOP_LOSS_PCT и др.

4. RiskManager (risk_manager.py)
   - Добавлена проверка ликвидности (требуем 3x volume)
   - Используется net_edge вместо gross spread

5. StrategySelector (strategy_selector.py)
   - Интегрирована RestOptimizedStrategy
   - Приоритет на rest_optimized если указано в config

📊 РЕАЛЬНЫЕ ОЖИДАНИЯ (капитал $1000):

Спредов 1%+/день:     30-60
После фильтров:       10-20
Открытых позиций:     3-5
Win Rate:             50-60%
Средний выигрыш:      +$0.40
Средний проигрыш:     -$0.30
Дневная прибыль:      $0.50-$2.00 (0.05-0.2%)
Месячная:             $15-$60 (1.5-6%)

🚀 ТЕСТИРОВАНИЕ:

1. Тест задержек:
   python test_rest_latency.py
   
   Ожидаемые результаты:
   - MEXC: 120-200ms
   - Bybit: 100-180ms
   - Gate.io: 150-250ms
   - Параллельное открытие: 150-250ms

2. Smoke test:
   python smoke_test.py
   
   Должен завершиться без ошибок AttributeError

3. Demo режим (неделя):
   python main.py
   
   Смотрите:
   - Сколько возможностей находится
   - Какой реальный win rate
   - Нужна ли подстройка порогов

⚙️ ОПТИМИЗАЦИЯ ПОРОГОВ:

Если слишком мало сделок (<3 в день):
→ OPEN_THRESHOLD = 0.8 (вместо 1.0)
→ MIN_NET_EDGE = 0.25 (вместо 0.3)

Если win rate <40%:
→ MIN_NET_EDGE = 0.4 (вместо 0.3)
→ OPEN_THRESHOLD = 1.2 (вместо 1.0)

Если много ложных сигналов:
→ MAX_DATA_AGE_MS = 300 (вместо 500)
→ SLIPPAGE_PCT = 0.08 (вместо 0.05)

🔧 ЗАПУСК LIVE:

1. Настройте .env с API ключами
2. В main.py: USE_LIVE_TRADING = True
3. Начните с $100-300
4. Мониторьте первые 24 часа
5. Оптимизируйте под вашу пару бирж

📚 ДОКУМЕНТАЦИЯ:

- docs/REST_TRADING_GUIDE.md — Полный гайд по REST торговле
- docs/LIVE_TRADING.md — Переход на live режим
- docs/QUICK_START.md — Быстрый старт

💡 ГЛАВНОЕ:

Это НЕ "get rich quick", это консервативная стратегия
для стабильного получения 1-6% в месяц.

REST торговля РАБОТАЕТ при правильных ожиданиях и настройках.

Удачи! 🚀
"""
